import { Typography } from '@mui/material';
import React, { useEffect } from 'react'

const TitleText = () => {

    return (
        <Typography variant='h1' style={{height: '150px', margin: '50px 0'}}>
            Hi! Welcome to viddychat!
        </Typography>);
};

export default TitleText;